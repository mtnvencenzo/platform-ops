apiVersion: v1
kind: Service
metadata:
  name: kafka-ext-ports
  namespace: kafka-platform
  labels:
    app.kubernetes.io/part-of: kafka-platform
spec:
  type: NodePort
  selector:
    app.kubernetes.io/part-of: kafka-platform
  ports:
    - name: kafka-broker-1
      port: 9092
      targetPort: 9092
      nodePort: 30092