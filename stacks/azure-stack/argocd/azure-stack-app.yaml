apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: azure-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/mtnvencenzo/platform-ops.git'
    targetRevision: main
    path: stacks/azure-stack/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: azure-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true