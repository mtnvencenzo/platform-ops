# === NVIDIA GPU (commented out) ===
# For Radeon GPU testing, use rocm-test.yml instead.
# To re-enable for NVIDIA, uncomment the lines below and comment out rocm-test.yml.
#
# apiVersion: v1
# kind: Pod
# metadata:
#   name: gpu-test
# spec:
#   restartPolicy: OnFailure
#   containers:
#   - name: cuda-container
#     image: nvidia/cuda:12.0.1-base-ubuntu22.04
#     command: ["nvidia-smi"]
#     resources:
#       limits:
#         nvidia.com/gpu: 1
